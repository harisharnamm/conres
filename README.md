# Resume Optimization Studio

Single-page Flask web app that optimizes uploaded one-page DOCX resumes for specific job descriptions while preserving document structure.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Then open http://localhost:8000.
